{
  "name": "fullstack-training",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "postinstall": "node scripts/fix-prisma-client.mjs",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "api:migrate": "pnpm --filter api exec prisma migrate dev",
    "api:studio": "pnpm --filter api exec prisma studio",
    "api:generate": "pnpm --filter api exec prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
